---
description: games / WordPress git submodule 旁路约定（源码子仓 vs 父仓 deploy）
globs: games/**/*,wordpress/**/*,deploy/games/**/*,deploy/wordpress/**/*,.gitmodules
alwaysApply: false
---

# Git Submodule 旁路约定

完整 SSOT：`.cursor/KNOWLEDGE_BASE.md` §7.0。编辑 `games/`、`wordpress/` 或相关 deploy 时遵守。

## 分层（禁止混淆）

| 层 | 路径 | 内容 |
|----|------|------|
| **Submodule 源码** | `games/<slug>/`、`wordpress/<slug>/` | 独立 Git 仓；父仓只存指针 |
| **父仓旁路** | `deploy/games/<slug>/`、`deploy/wordpress/` | nginx、compose 模板、ADD-* 文档、CI 生成的 `www/` |
| **主站导航** | `apps/web/src/modules/games/` | 大厅卡片与筛选；**不是** Godot 源码 |

- `deploy/wordpress/` = 全站 WP **基建文档**，**不是** submodule。
- `deploy/games/<slug>/www/` = 导出产物，**不进 git**。

## 开发规则

1. 改主题/游戏逻辑 → 进入 submodule 目录，在 **子仓** commit/push，再回到父仓 bump submodule 指针。
2. 克隆父仓：`git submodule update --init --recursive`（CI 已 `submodules: recursive`）。
3. 新增旁路 → 先建独立仓，`git submodule add …`，再改 compose/nginx/smoke/导航；见 `deploy/games/ADD-GAME.md` 或 `deploy/wordpress/ADD-SITE.md`。
4. 从 Next 迁出 Godot：删 `apps/web` 旧模块；DB schema 迁到 `packages/db/src/schema/`（若表仍保留）。
5. 实验田/大厅链到 `/games/*`、`/wp/*` → **整页跳转**（`ExperimentNavCard`），勿用 Next 客户端路由。

## CI / 部署

- `GAMES_CHANGED`：导出并 scp `www/`；未变更保留服务器已有包。
- `WORDPRESS_CHANGED`：scp `wordpress/<slug>/*` 到服务器；未变更不覆盖主题目录。
- 每次 deploy 同步 `deploy/**`（含 `deploy/wordpress/` 文档目录）。

## 禁止

- 在父仓直接改 submodule 内文件却不更新子仓与指针。
- 把 `deploy/wordpress/` 当作 Holt 主题源码目录。
- 将 Godot 源码放进 `apps/web/src/modules`。
