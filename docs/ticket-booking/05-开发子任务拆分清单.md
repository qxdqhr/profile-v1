# 05 — 开发子任务拆分清单

> 不写排期；勾选表示 MVP 交付内应完成。

## A. 文档与设计

- [x] A1：编写 01～05 需求/PRD/技术/数据/任务文档（本目录）
- [x] A2：Pencil 线框导出 `design/ticket-booking-mvp.png` + `.pen`

## B. 模块与路由

- [x] B1：创建 `src/modules/ticketBooking` 目录与 `DEVELOPMENT.md`
- [x] B2：实现类型与 `mockData`（演出列表 + 每场的已售座位集）
- [x] B3：实现 `PerformanceList`、`SeatMapGrid` 组件
- [x] B4：实现 `TicketBookingMvpPage` 三步状态机
- [x] B5：`src/app/(pages)/testField/(cyhj)/TicketBooking/page.tsx` 薄封装
- [x] B6：`experimentData.ts` 注册入口

## C. 质量

- [x] C1：`pnpm exec tsc --noEmit` 或通过项目既有 lint 命令自检修改文件
- [x] C2：移动端宽度下选座网格可滚动/不挤压破版

## D. 后续迭代（不在 MVP PR）

- [ ] D1：Drizzle schema + 迁移 + API 转发
- [ ] D2：锁座与过期、订单状态机
- [ ] D3：对接真实支付沙箱
